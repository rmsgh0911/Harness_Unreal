# Cycle Log Example

Use real cycle logs only inside `Harness/<Worker>/work/cycles/YYYY-MM-DD.md` while work is active. Do not ship real cycle logs in a clean template package.

```markdown
## HH:MM Task Name
- Changed:
- Verified:
- Remaining:
```
