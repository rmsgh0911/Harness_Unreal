# Next

Project-level unresolved work only. Put branch ownership, agent names, and per-edit timestamps in `Harness/work/tasks/`.

## Active Work

- Fill `Harness/config/project.json` for this project.
- Confirm project docs under `Harness/docs/` or register an external folder.
- Update `Harness/work/state.md` from actual Source, Config, asset, and build evidence.
- Run `Harness/scripts/unreal/verify_project.py` and adapt checks to the real project.
- Fill build configuration when C++ or module work is expected.
