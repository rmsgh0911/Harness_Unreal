# Next

Project-level unresolved work only. Put branch ownership, agent names, and per-edit timestamps in `Harness/work/tasks/`.

## Immediate Setup Work

- Fill `Harness/config/project.json` for this project.
- Keep project docs under `Harness/docs/` by default.
- Update `Harness/work/state.md` from actual Source, Config, and asset evidence.
- Create a task record under `Harness/work/tasks/` for parallel work, and prefer a task-scoped cycle file.
- Run `Harness/scripts/unreal/verify_project.py` and adapt required checks to the real project.
- Fill build configuration when C++ or module work is expected.

## Feature Work

- TODO: Add feature-sized tasks with success criteria and maximum cycle count when relevant.

## Manual Verification

- TODO: Record PIE checks that automation cannot prove.

## Known Issues

- TODO
